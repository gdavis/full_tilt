- content_for :head do
  %title FullTiLT
  :javascript
    $(document).ready(function() {
      var galleryContainer = $('.left-right-gallery');
      console.log('number of gallery containers: ' + galleryContainer.length );
      galleryContainer.each(function( $index, $el ){
        console.log('index: ' +$index );
        new $.LeftRightGallery( $el, 950, 350 );
      })
    });

%h1 Page.
#gallery
  - data.projects['entries'].each do |entry|
    = render 'entry', :locals => { :data => entry }